require 'spec_helper'

describe Listen::Change do

  # describe "#_notify_change" do
  #   let(:mailbox) { mock('mailbox') }
  #   before { listener.stub(:mailbox) { mailbox } }

  #   it "adds changed path to listener actor mailbox" do
  #     mailbox.should_receive(:<<).with(['path'])
  #     adapter.send(:_notify_changes, 'path')
  #   end

  #   it "adds changed paths to listener actor mailbox" do
  #     mailbox.should_receive(:<<).with(['path1', 'path2'])
  #     adapter.send(:_notify_changes, ['path1', 'path2'])
  #   end
  # end


end
